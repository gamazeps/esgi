#include <vector>


int main() {
    // Vector
    vector<int> vec;
    
    vec.push_back(1);
    vec.push_back(2);
    vec.push_back(3);
    vec.push_back(4);

    vec[2]; 

    // NOte the asymptotic constant time.

    map<string, int> wc;

    wc["hello"]++;

    int titi = wc["world"];
}
